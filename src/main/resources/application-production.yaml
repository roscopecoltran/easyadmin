server:
  port: 8080

jwt:
  header: Authorization
  secret: mySecret
  expiration: 604800
  route:
    authentication:
      path: auth
      refresh: refresh

spring:
  data:
    mongodb:
      uri: mongodb://admin:admin@ds161443.mlab.com:61443/easyadmin
      database: easyadmin